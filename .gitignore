# Python bytecode from tools/
__pycache__/
*.pyc

# Claude Code's per-machine settings. It carries an absolute path from
# whoever's machine wrote it, Claude Code writes it without being asked, and
# it lands untracked exactly where `git add -A` would sweep it up.
.claude/settings.local.json
